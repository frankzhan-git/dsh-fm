# dsh-fm bundle patch — 作为 profile bundle 层应用
# name 即 loader 的模块说明符，必须与 npm 包名 dsh-fm 完全一致（并与 scripts/build.mjs 的 banner id 同步）；
# 插件管理器显示名由官方按 dsh- 前缀剥离得到（显示为 "fm"）。
- insert:
    - id: dsh-fm
      name: dsh-fm
